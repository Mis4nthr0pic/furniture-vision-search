# Screenshots

Capture these from a local demo run for the main [README](../README.md).

1. **search-upload.png** — Search page with a furniture photo uploaded and optional prompt filled.
2. **vision-features.png** — Vision sidebar showing category, type, confidence percentages.
3. **ranked-results.png** — At least two result cards with hybrid score, rerank reason, and rank badges.
4. **admin-config.png** — Admin → Config with model settings and retrieval weights visible.
5. **admin-eval.png** — Static Eval summary cards or Live Eval metrics after a session.
6. **reindex-progress.png** — Reindex progress modal mid-run (batch progress bar).

Recommended size: 1440px wide browser window, light mode, no personal API keys visible.
