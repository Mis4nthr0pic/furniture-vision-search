# README engineering narrative (2026-05-21)

**Change:** Root README now leads with engineering judgment — honest eval baseline (1/6 passed, weak type/color), failure modes, what improved after testing, and explicit note that UI is inspection tooling not the product.

**Fix:** Removed stale eval table (67% type / 0.583 MRR from mislabeled fixtures) — aligned with `docs/EVAL.md` and `CHANGELOG.md`.

**Deploy:** README states no hosted demo yet; points to `docs/DEPLOYMENT.md`.
